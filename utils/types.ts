export type GuildDetails = {
  description: string;
  id: number;
  imageUrl: string;
  name: string;
  urlName: string;
};

export type HatDetails = {
  imageUrl: string;
  ipId: string;
  name: string;
  wearers: string[];
};
